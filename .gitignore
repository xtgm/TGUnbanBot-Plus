# Wrangler 本地缓存
.wrangler/
.dev.vars
.dev.vars.*

# Node
node_modules/
package-lock.json

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log

# 历史备份
# 用 *.bak* 而非 *.bak：分阶段开发的快照实际后缀是 .bak-p2b / .bak-p10 这类带阶段号的形态，
# 精确后缀 *.bak 匹配不上，12 个备份一直挂在 git status 的未跟踪列表里干扰查看真实改动。
*.bak*
